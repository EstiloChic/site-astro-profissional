<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enrique Enguix - Soluções de Trading Quantitativo</title>

    <!-- SEO e Meta Tags Melhoradas -->
    <meta name="description" content="Enrique Enguix - Desenvolvimento de Expert Advisors (Robôs de Trading) para MQL5 com base em análise quantitativa e inteligência artificial.">
    <meta name="keywords" content="Enrique Enguix, MQL5, Expert Advisor, Trading Quantitativo, Trading Algorítmico, Forex, IA, Nexus EA, AXIS Neural EA, NODE Neural EA">
    <meta property="og:title" content="Enrique Enguix - Soluções de Trading Quantitativo">
    <meta property="og:description" content="Desenvolvimento de EAs para MQL5 que unem análise quantitativa e tecnologia para otimizar os seus resultados.">
    <meta property="og:image" content="https://i.postimg.cc/mgspR5D0/C-pia-de-Beige-Minimalist-Initial-Brand-Logo-Instagram-Post-1.png">
    <meta property="og:url" content="https://www.seusite.com"> <!-- Substituir pelo seu URL final -->
    <meta property="og:type" content="website">

    <link rel="icon" href="favicon.ico" type="image/x-icon">
    <link rel="apple-touch-icon" href="apple-touch-icon.png">

    <!-- FONTES -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&family=Roboto:wght@400;500&display=swap" rel="stylesheet">
    
    <!-- BIBLIotecas -->
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- Link para nosso arquivo CSS externo -->
    <link rel="stylesheet" href="/styles/style.css">
</head>

<body>
    <header class="main-nav">
        <a href="#inicio" class="logo">Enrique Enguix</a>
        <div class="desktop-nav">
            <nav>
                <ul class="nav-links">
                    <li><a href="#produtos"><span class="lang lang-pt">Produtos</span><span class="lang lang-es">Productos</span><span class="lang lang-en">Products</span></a></li>
                    <li><a href="#blog"><span class="lang lang-pt">Blog</span><span class="lang lang-es">Blog</span><span class="lang lang-en">Blog</span></a></li>
                    <li><a href="#sobre"><span class="lang lang-pt">Sobre</span><span class="lang lang-es">Sobre</span><span class="lang lang-en">About</span></a></li>
                    <li><a href="#contato"><span class="lang lang-pt">Contato</span><span class="lang lang-es">Contacto</span><span class="lang lang-en">Contact</span></a></li>
                </ul>
            </nav>
            <div class="language-switcher">
                <button id="language-toggle-button" aria-label="Choose language"><i class="fas fa-globe"></i></button>
                <ul id="language-menu">
                    <li><button data-lang="pt"><span class="flag">🇧🇷</span> <span class="text">Português</span></button></li>
                    <li><button data-lang="es"><span class="flag">🇪🇸</span> <span class="text">Español</span></button></li>
                    <li><button data-lang="en"><span class="flag">🇬🇧</span> <span class="text">English</span></button></li>
                </ul>
            </div>
        </div>
        <button id="mobile-menu-toggle" aria-label="Open menu">
            <i class="fas fa-bars icon-bars"></i>
            <i class="fas fa-times icon-close"></i>
        </button>
    </header>

    <div class="mobile-nav-container" id="mobile-nav-container">
        <nav>
            <ul class="nav-links">
                <li><a href="#produtos"><span class="lang lang-pt">Produtos</span><span class="lang lang-es">Productos</span><span class="lang lang-en">Products</span></a></li>
                <li><a href="#blog"><span class="lang lang-pt">Blog</span><span class="lang lang-es">Blog</span><span class="lang lang-en">Blog</span></a></li>
                <li><a href="#sobre"><span class="lang lang-pt">Sobre</span><span class="lang lang-es">Sobre</span><span class="lang lang-en">About</span></a></li>
                <li><a href="#contato"><span class="lang lang-pt">Contato</span><span class="lang lang-es">Contacto</span><span class="lang lang-en">Contact</span></a></li>
            </ul>
        </nav>
        <div class="language-switcher">
            <button id="mobile-language-toggle-button" aria-label="Choose language"><i class="fas fa-globe"></i></button>
            <ul id="mobile-language-menu">
                <li><button data-lang="pt"><span class="flag">🇧🇷</span> <span class="text">Português</span></button></li>
                <li><button data-lang="es"><span class="flag">🇪🇸</span> <span class="text">Español</span></button></li>
                <li><button data-lang="en"><span class="flag">🇬🇧</span> <span class="text">English</span></button></li>
            </ul>
        </div>
    </div>
    
    <main>
        <!-- O conteúdo da página será inserido aqui -->
        <slot />
    </main>

    <a href="#inicio" id="back-to-top" aria-label="Voltar ao topo"><i class="fas fa-chevron-up"></i></a>

    <footer>
        <div class="social-links" style="margin-bottom: 1.5rem;">
            <a href="https://www.mql5.com/pt/users/envex/seller" target="_blank" rel="noopener noreferrer" aria-label="Perfil MQL5"><i class="fas fa-chart-line"></i></a>
            <a href="https://www.linkedin.com/" target="_blank" rel="noopener noreferrer" aria-label="Perfil LinkedIn"><i class="fab fa-linkedin"></i></a>
            <a href="mailto:enguix67@gmail.com" target="_blank" rel="noopener noreferrer" aria-label="Enviar E-mail"><i class="fas fa-envelope"></i></a>
        </div>
        <p><span class="lang lang-pt">&copy; 2025 Enrique Enguix. Todos os direitos reservados.</span><span class="lang lang-es">&copy; 2025 Enrique Enguix. Todos los derechos reservados.</span><span class="lang lang-en">&copy; 2025 Enrique Enguix. All rights reserved.</span></p>
    </footer>

    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    <script is:inline>
        AOS.init({ duration: 800, once: true, delay: 100, anchorPlacement: 'top-bottom' });

        document.addEventListener('DOMContentLoaded', () => {
            const backToTopButton = document.getElementById('back-to-top');
            window.addEventListener('scroll', () => {
                if (window.scrollY > 300) {
                    backToTopButton.classList.add('show');
                } else {
                    backToTopButton.classList.remove('show');
                }
            });

            // --- Lógica do Menu Mobile ---
            const mobileMenuToggle = document.getElementById('mobile-menu-toggle');
            const mobileNavContainer = document.getElementById('mobile-nav-container');
            const closeMobileMenu = () => document.body.classList.remove('mobile-menu-open');
            mobileMenuToggle.addEventListener('click', (e) => { e.stopPropagation(); document.body.classList.toggle('mobile-menu-open'); });
            mobileNavContainer.querySelectorAll('a').forEach(link => link.addEventListener('click', closeMobileMenu));
            document.addEventListener('click', (e) => {
                if (document.body.classList.contains('mobile-menu-open') && !mobileNavContainer.contains(e.target) && !mobileMenuToggle.contains(e.target)) {
                    closeMobileMenu();
                }
            });

            // --- Lógica do Seletor de Idiomas ---
            const htmlElement = document.documentElement;
            const langData = {
                pt: { title: 'Enrique Enguix - Soluções de Trading Quantitativo' },
                es: { title: 'Enrique Enguix - Soluciones de Trading Cuantitativo' },
                en: { title: 'Enrique Enguix - Quantitative Trading Solutions' }
            };
            const setLanguage = (lang) => {
                if (!langData[lang]) return;
                const isMenuOpen = document.body.classList.contains('mobile-menu-open');
                document.body.className = Array.from(document.body.classList).filter(c => !c.startsWith('lang-active-')).join(' ');
                document.body.classList.add(`lang-active-${lang}`);
                if (isMenuOpen) document.body.classList.add('mobile-menu-open');
                htmlElement.setAttribute('lang', lang);
                document.title = langData[lang].title;
                localStorage.setItem('language', lang);
            };
            const setupLanguageSwitcher = (toggleBtnId, menuId) => {
                const toggleButton = document.getElementById(toggleBtnId);
                const languageMenu = document.getElementById(menuId);
                if (!toggleButton || !languageMenu) return;
                toggleButton.addEventListener('click', (e) => { e.stopPropagation(); languageMenu.classList.toggle('show'); });
                languageMenu.addEventListener('click', (e) => {
                    const button = e.target.closest('button');
                    if (button && button.dataset.lang) { setLanguage(button.dataset.lang); languageMenu.classList.remove('show'); }
                });
            };
            setupLanguageSwitcher('language-toggle-button', 'language-menu');
            setupLanguageSwitcher('mobile-language-toggle-button', 'mobile-language-menu');
            document.addEventListener('click', (e) => {
                if (!e.target.closest('.language-switcher')) {
                    document.getElementById('language-menu')?.classList.remove('show');
                    document.getElementById('mobile-language-menu')?.classList.remove('show');
                }
            });
            const savedLang = localStorage.getItem('language') || 'pt';
            setLanguage(savedLang);

            // --- LÓGICA DE NAVEGAÇÃO SPA (SINGLE PAGE APP) ---
            const mainContent = document.querySelector('.main-content');
            const pageViews = document.querySelector('.page-views');
            const allPageViews = pageViews.querySelectorAll('.page-view');
            const handleNavigation = () => {
                const hash = window.location.hash.substring(1);
                const targetView = document.getElementById(hash);
                window.scrollTo(0, 0);
                if (targetView && pageViews.contains(targetView)) {
                    mainContent.style.display = 'none';
                    allPageViews.forEach(view => view.style.display = 'none');
                    targetView.style.display = 'block';
                    updateSocialShareLinks();
                } else {
                    mainContent.style.display = 'block';
                    allPageViews.forEach(view => view.style.display = 'none');
                    if (hash && hash !== 'inicio') {
                       const mainSection = document.getElementById(hash);
                       if (mainSection) { setTimeout(() => mainSection.scrollIntoView({ behavior: 'smooth' }), 0); }
                    }
                }
            };
            window.addEventListener('hashchange', handleNavigation);
            handleNavigation();

            // --- LÓGICA DAS ABAS DO BLOG ---
            const blogTabsContainer = document.querySelector('.blog-tabs');
            if (blogTabsContainer) {
                const tabButtons = blogTabsContainer.querySelectorAll('.tab-button');
                const tabContents = document.querySelectorAll('.tab-content');
                tabButtons.forEach(button => {
                    button.addEventListener('click', () => {
                        tabButtons.forEach(btn => btn.classList.remove('active'));
                        tabContents.forEach(content => content.classList.remove('active'));
                        button.classList.add('active');
                        document.getElementById(`tab-${button.dataset.tab}`).classList.add('active');
                    });
                });
            }

            // --- LÓGICA DAS ABAS DE CONTATO ---
            const contactOptionsContainer = document.querySelector('.contact-options');
            if (contactOptionsContainer) {
                const contactButtons = contactOptionsContainer.querySelectorAll('.tab-button');
                const contactDetails = document.querySelectorAll('.contact-details');
                contactButtons.forEach(button => {
                    button.addEventListener('click', () => {
                        contactButtons.forEach(btn => btn.classList.remove('active'));
                        contactDetails.forEach(detail => detail.classList.remove('active'));
                        button.classList.add('active');
                        document.getElementById(`contact-${button.dataset.contact}`).classList.add('active');
                    });
                });
            }

            // --- LÓGICA DO FORMULÁRIO DE CONTATO (SEM RECARREGAR) ---
            const contactForm = document.getElementById('contact-form-element');
            const feedbackMessage = document.getElementById('form-feedback-message');
            contactForm.addEventListener('submit', function (e) {
                e.preventDefault();
                const submitButton = contactForm.querySelector('button[type="submit"]');
                const originalButtonText = submitButton.innerHTML;
                submitButton.disabled = true;
                submitButton.innerHTML = `<i class="fas fa-spinner fa-spin"></i> <span class="lang lang-pt">Enviando...</span><span class="lang lang-es">Enviando...</span><span class="lang lang-en">Sending...</span>`;

                setTimeout(() => {
                    contactForm.reset();
                    submitButton.disabled = false;
                    submitButton.innerHTML = originalButtonText;
                    feedbackMessage.textContent = 'Mensagem enviada com sucesso! Obrigado.';
                    feedbackMessage.className = 'form-feedback success';
                    feedbackMessage.style.display = 'block';
                    
                    setTimeout(() => { feedbackMessage.style.display = 'none'; }, 5000);
                }, 1500);
            });
            
            // --- LÓGICA DOS BOTÕES DE COMPARTILHAMENTO ---
            function updateSocialShareLinks() {
                const pageUrl = window.location.href;
                const pageTitle = document.title;
                const shareLinks = document.querySelectorAll('.social-share-buttons');
                
                shareLinks.forEach(container => {
                    const twitterShare = container.querySelector('.share-twitter');
                    if (twitterShare) twitterShare.href = `https://twitter.com/intent/tweet?text=${encodeURIComponent(pageTitle)}&url=${encodeURIComponent(pageUrl)}`;
                    
                    const linkedinShare = container.querySelector('.share-linkedin');
                    if (linkedinShare) linkedinShare.href = `https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(pageUrl)}`;
                    
                    const facebookShare = container.querySelector('.share-facebook');
                    if (facebookShare) facebookShare.href = `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(pageUrl)}`;

                    const telegramShare = container.querySelector('.share-telegram');
                    if (telegramShare) telegramShare.href = `https://t.me/share/url?url=${encodeURIComponent(pageUrl)}&text=${encodeURIComponent(pageTitle)}`;
                });
            }
        });
    </script>
</body>
</html>
